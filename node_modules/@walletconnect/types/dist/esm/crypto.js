export class IKeyChain {
    constructor(client, logger) {
        this.client = client;
        this.logger = logger;
    }
}
export class ICrypto {
    constructor(client, logger, keychain) {
        this.client = client;
        this.logger = logger;
    }
}
//# sourceMappingURL=crypto.js.map